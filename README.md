# JWFL media host

Public repo for Meta-fetchable JWFL reel binaries.

## Intended overnight bridge (2026-09-06)
1. Push MP4 base64 chunks here (or binary via release assets).
2. Composio REMOTE_WORKBENCH: download + decode.
3. `upload_local_file` → `s3key`.
4. `INSTAGRAM_POST_IG_USER_MEDIA` with `video_file.s3key` (no public URL needed) **or** deploy to `jwfl-media.vercel.app`.

Samovar Party (CC BY) baked into API reels.
Account: jewishwisdomforlife / `instagram_wiggle-unskin`.
